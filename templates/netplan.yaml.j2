network:
    version: 2
    ethernets:
        eth0:
            addresses: [{{ network_addr }}]
            gateway4: {{ network_gateway }}
            nameservers:
                addresses: [{{ nameservers | join(', ') }}]
            dhcp4: false
            dhcp6: false
        wlan0:
            addresses: [{{ wifi_net | ipaddr('net') | ipaddr('1') }}]
            dhcp4: false
            dhcp6: false
